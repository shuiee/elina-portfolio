---
/* Numbering is earned here and only here: these runs are genuinely ordered.
   Do not use numbered markers as decoration anywhere else on the site. */
---
<ol class="method"><slot /></ol>
<style>
  .method { list-style: none; padding: 0; margin: var(--space-6) 0 0; display: grid; gap: var(--space-8); }
</style>
